# Production environment — never commit the real values
NODE_ENV=production
PORT=3000
JWT_SECRET=replace-with-a-long-random-secret
DATABASE_URL=postgresql://mkpos:change-me@postgres:5432/mkpos
CORS_ORIGINS=https://pos.example.com,https://order.example.com
TRUST_PROXY=1
