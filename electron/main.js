const {app,BrowserWindow}=require('electron');const path=require('path');const {spawn}=require('child_process');let server;function create(){const w=new BrowserWindow({width:1440,height:900,webPreferences:{contextIsolation:true}});w.loadURL('http://127.0.0.1:3000');}app.whenReady().then(()=>{server=spawn(process.execPath,[path.join(__dirname,'..','server.js')],{env:{...process.env,PORT:'3000'},stdio:'ignore'});setTimeout(create,1000)});app.on('window-all-closed',()=>{if(server)server.kill();if(process.platform!=='darwin')app.quit()});